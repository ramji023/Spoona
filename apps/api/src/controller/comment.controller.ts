import { Request,Response } from "express"

// get all the comments on recipe controller
export const getAllComment = (req:Request,res:Response)=>{
    return 
}

// make comment controller
export const makeComment= (req:Request,res:Response)=>{
    return
}